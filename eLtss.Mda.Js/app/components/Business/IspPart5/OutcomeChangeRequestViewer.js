import React, {PropTypes} from 'react';
const OutcomeChangeRequestViewer = (test) => {

    return (
        <span>This is OutcomeChangeRequestViewer...</span>
    );
};

OutcomeChangeRequestViewer.propTypes = {
    //plans: PropTypes.array
};

export default OutcomeChangeRequestViewer;
